hydra:
  run:
    dir: .
  sweep:
    dir: .
    subdir: .
  job_logging:
    root:
      level: INFO

# Global variables 
seed: 0
save_dir: "./exp/${wandb.group}/${wandb.name}"
debug: False
save: True
save_interval: 100


# Logging setup
wandb:
  entity: "ucl-dark"
  project: ipd
  group: ??
  name: run-seed-${seed}
  log: True
